import { Plus_Jakarta_Sans } from "next/font/google";

/**
 * Primary UI font — Plus Jakarta Sans (Figma).
 */
export const plusJakartaSans = Plus_Jakarta_Sans({
  variable: "--font-plus-jakarta-sans",
  subsets: ["latin"],
  display: "swap",
});
