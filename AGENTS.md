# Agent notes

This is a Vite + React SPA (not Next.js). Prefer React Router for navigation, Vite `import.meta.env` for env vars, and standard React patterns. Keep the existing Tailwind design tokens and admin UI intact unless asked to redesign.
